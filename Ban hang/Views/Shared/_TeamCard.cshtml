@model TeamMember
<div class="team-card">
    <img src="@Model.AvatarUrl" alt="@Model.Name" loading="lazy" />
    <h4>@Model.Name</h4>
    <div class="role">@Model.Role</div>
    <p>@Model.Bio</p>
</div>
